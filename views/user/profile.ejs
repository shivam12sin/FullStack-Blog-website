<div class="admin-title">
  <h2>Profile Settings</h2>
</div>

<div class="auth-container" style="max-width: 100%; padding: 2rem;">
  <form action="/profile?_method=PUT" method="POST">
    <label for="email"><b>Email Address</b> <span style="font-weight: normal; color: var(--text-muted); font-size: 0.85em;">(Required for network notifications)</span></label>
    <input type="email" name="email" placeholder="you@domain.com" value="<%= user.email || '' %>" style="width: 100%; margin-bottom: 1.5rem; padding: 0.75rem; border: 1px solid var(--border); border-radius: 4px;">

    <label for="bio"><b>Author Biography</b></label>
    <textarea name="bio" placeholder="Tell your readers about yourself..."><%= user.bio %></textarea>

    <input type="submit" value="Save Profile" class="btn" style="width: 200px;">
  </form>
</div>

<section class="article-header">

  <h1 class="display-lg article__title"><%= data.title %></h1>
  <div class="article-header__meta">
    <% if (data.author) { %>
      <a href="/author/<%= data.author._id %>" class="article-author-tag">Written by <%= data.author.username %></a>
    <% } %>
    <span class="label-md"><%= data.createdAt.toDateString() %> · <%= readingTime %> min read</span>
  </div>
  <% if (data.tags && data.tags.length > 0) { %>
    <div class="article-taglist">
      <% data.tags.forEach(tag => { %>
        <a href="/tag/<%= tag %>" class="tag-chip"><%= tag %></a>
      <% }) %>
    </div>
  <% } %>

</section>

<article class="article article-body">
  <%- content %>
</article>

  <div class="upvote-bar" style="margin-bottom: 2rem;">
    <% if (typeof userId !== 'undefined' && userId) { %>
      <form action="/post/<%= data._id %>/upvote" method="POST" class="upvote-form">
        <button type="submit" class="upvote-btn <%= hasUpvoted ? 'upvoted' : '' %>" aria-label="Upvote this post">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M12 4L3 15h6v5h6v-5h6L12 4z" stroke="currentColor" stroke-width="2" stroke-linejoin="round" fill="<%= hasUpvoted ? 'currentColor' : 'none' %>"/>
          </svg>
          <span><%= upvoteCount %></span>
        </button>
      </form>
    <% } else { %>
      <a href="/login" class="upvote-btn upvote-btn--disabled" aria-label="Log in to upvote">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <path d="M12 4L3 15h6v5h6v-5h6L12 4z" stroke="currentColor" stroke-width="2" stroke-linejoin="round"/>
        </svg>
        <span><%= upvoteCount %></span>
      </a>
    <% } %>
  </div>

<section class="comments-section">
  <div class="section-heading">

    <h3>Discussion (<%= comments ? comments.length : 0 %>)</h3>
  </div>

  <% if (typeof userId !== 'undefined' && userId) { %>
    <form action="/post/<%= data._id %>/comment" method="POST" class="comment-form">
      <textarea name="content" rows="3" placeholder="What are your thoughts?" required></textarea>
      <div class="comment-form__actions">
        <button type="submit" class="btn">Post Comment</button>
      </div>
    </form>
  <% } else { %>
    <div class="comment-callout">
      <p>Join the conversation.</p>
      <a href="/login">Log in or create an account →</a>
    </div>
  <% } %>

  <div class="comments-list">
    <% if (comments && comments.length > 0) { %>
      <% comments.forEach(comment => { %>
        <div class="comment">
          <div class="comment__meta">
            <a href="/author/<%= comment.author._id %>"><%= comment.author.username %></a>
            <span><%= comment.createdAt.toLocaleDateString() %></span>
          </div>
          <p><%= comment.content %></p>
        </div>
      <% }) %>
    <% } else { %>
      <p class="comment-empty">No comments yet. Be the first to share your thoughts!</p>
    <% } %>
  </div>
</section>
